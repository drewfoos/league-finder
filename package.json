{
  "name": "league-finder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "go-build": "go build -o goserver.exe ./api",
    "go-run": "goserver.exe",
    "deploy": "vercel --prod" 
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "vercel": "^32.1.0"
  }
}
